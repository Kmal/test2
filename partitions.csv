# Name,   Type, SubType, Offset,   Size,     Flags
# Keep the app slot larger than ESP-IDF's default 1 MiB factory partition; the
# StickS3 sound-meter build includes BLE, Wi-Fi/web setup, LCD UI, and rule
# automation and is currently larger than 1 MiB.
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x300000,
