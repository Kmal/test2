# Name,   Type, SubType, Offset,   Size,     Flags
# Use the remainder of the 8 MB flash for the factory app slot after the
# bootloader, partition table, NVS, and PHY data regions.  The generated
# factory image is currently much smaller than this, but the larger slot lets
# maintainers compile additional StickS3 modules without repartitioning.
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x7F0000,
